def main() -> None:
    raise SystemExit(
        "LSTM is intentionally left as a second-phase experiment. "
        "Install tensorflow and add a sequence model after the tabular baseline is validated."
    )


if __name__ == "__main__":
    main()

